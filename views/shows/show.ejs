<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/assets/css/styles.css"/>
  <title><%= show.title %> Show Page</title>
</head>
<body>
  <a href="/shows/<%=show._id%>/edit">Edit <%= show.title %></a>
  <h1><%= show.title %>'s Activity</h1>
  <h2>Post's Written by <%= show.title %></h2>
  <ul>
    <% show.posts.forEach((post) => { %>
      <li>
        <a href="/posts/<%=post._id%>">
            <%= post.title %> by <%= post.year %>
        </a>
      </li>
    <% }) %>
  </ul>
  <h2>Comment's Made by <%= show.title %></h2>
  <ul>
    <% show.comments.forEach((comment) => { %>
      <li>
        <a href="/posts/<%=comment.post%>">
         Comment by <%= show.title%> on  <%= comment.post %> that reads  <%= comment.content %>
        </a>
      </li>
    <% }) %>
  </ul>
</body>
</html>